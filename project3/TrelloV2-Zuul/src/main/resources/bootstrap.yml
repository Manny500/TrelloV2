spring:
  application:
    name: zuul
  datasource:
   tomcat:
     initial-size: 2
         
server:
    port: 8765
   
zuul:
 routes:
   authServer:
     path: /auth-service/**
     sensitiveHeaders:
     serviceId: auth-service  
   portalEndpoint:
     path: /portal/**
     sensitiveHeaders:
     serviceId: portal  
 